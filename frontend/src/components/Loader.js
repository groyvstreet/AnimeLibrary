import logo from "../logo.svg";

const Loader = () => {
    return (
        <div>
            <img className="App-logo" src={logo} alt=""/>
            <h2>Загрузка...</h2>
        </div>
    )
}

export default Loader
